import { AlreadyExistsValidatorDirective } from './already-exists-validator.directive';

describe('AlreadyExistsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AlreadyExistsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
